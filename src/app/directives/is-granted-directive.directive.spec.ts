import { IsGrantedDirectiveDirective } from './is-granted-directive.directive';

describe('IsGrantedDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new IsGrantedDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
